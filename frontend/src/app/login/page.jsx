import Login from "@/components/login/Login";
import { cookies } from "next/headers";

const Form = () => {
    return (
        <section>
            <Login />
        </section>
    );
};

export default Form;